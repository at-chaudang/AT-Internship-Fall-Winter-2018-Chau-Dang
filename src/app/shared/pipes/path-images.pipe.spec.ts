import { PathImagesPipe } from './path-images.pipe';

describe('PathImagesPipe', () => {
  it('create an instance', () => {
    const pipe = new PathImagesPipe();
    expect(pipe).toBeTruthy();
  });
});
